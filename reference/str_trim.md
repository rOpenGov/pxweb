# Taken from `trimws` for reasons of compatibility with previous R versios.

Taken from `trimws` for reasons of compatibility with previous R
versios.

## Usage

``` r
str_trim(x, which = c("both", "left", "right"))
```

## Arguments

- x:

  a string to trim.

- which:

  how to trim the string.

## See also

trimws
