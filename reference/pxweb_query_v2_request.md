# Construct a PXWEB API v2 request object.

Construct a PXWEB API v2 request object.

## Usage

``` r
pxweb_query_v2_request(body, extra_query = list())
```

## Arguments

- body:

  a `pxweb_query_v2` object.

- extra_query:

  additional URL query parameters.
