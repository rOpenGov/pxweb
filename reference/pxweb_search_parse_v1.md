# Parse PXWEB API v1 search results

Parse PXWEB API v1 search results

## Usage

``` r
pxweb_search_parse_v1(x, api_url)
```

## Arguments

- x:

  a parsed PXWEB API v1 search response.

- api_url:

  the PXWEB API v1 database root URL used for the search.
