# Convert a `pxweb_query` object to a PXWEB API v2 query.

Convert a `pxweb_query` object to a PXWEB API v2 query.

## Usage

``` r
pxweb_query_as_v2(pxq)
```

## Arguments

- pxq:

  a `pxweb_query` object.

## Value

a `pxweb_query_v2` object.
