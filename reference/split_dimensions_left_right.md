# Split variables into chunks

Split variables into chunks

## Usage

``` r
split_dimensions_left_right(x, bool, max_size)
```

## Value

a `pxweb_split_dimensions`

## Details

Splitable variables are variables that can be split. Content variables
cannot be split, not variables with filter == "top"
