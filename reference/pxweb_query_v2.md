# Construct a `pxweb_query_v2` object.

Construct a `pxweb_query_v2` object.

## Usage

``` r
pxweb_query_v2(x)
```

## Arguments

- x:

  a list with a PXWEB API v2 `selection` body.

## Value

a `pxweb_query_v2` object.
