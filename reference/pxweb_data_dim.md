# Compute the dimension of the query `pxweb_data` object

Compute the dimension of the query `pxweb_data` object

## Usage

``` r
pxweb_data_dim(pxd)
```

## Arguments

- pxd:

  a `pxweb_data` object.
