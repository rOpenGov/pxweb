# Assert that x is a correct `pxweb_data_comments` object.

Assert that x is a correct `pxweb_data_comments` object.

## Usage

``` r
assert_pxweb_data_comments(x)
```

## Arguments

- x:

  an object to check.
