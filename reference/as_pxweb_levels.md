# Assert that x is a correct `pxweb_database_list` object.

Assert that x is a correct `pxweb_database_list` object.

## Usage

``` r
as_pxweb_levels(x)
```

## Arguments

- x:

  an object to check.
