# Construct a `pxweb_metadata` object.

An object that contain the metadata for a given PXWEB table.

## Usage

``` r
pxweb_metadata(x)
```

## Arguments

- x:

  a list returned from a PXWEB API to convert to a `pxweb_metadata`
  object.

## Value

a `pxweb_metadata` object
