# Assert that x is a correct `pxweb_data_jsonstat` object. Assert a json-stat version 1.0 or later object

Assert that x is a correct `pxweb_data_jsonstat` object. Assert a
json-stat version 1.0 or later object

## Usage

``` r
assert_pxweb_data_jsonstat(x)
```

## Arguments

- x:

  an object to check.
