# Construct a `pxweb_levels` object.

An object that contain the levels for a given PXWEB api position.

## Usage

``` r
pxweb_levels(x)
```

## Arguments

- x:

  a list returned from a PXWEB API to convert to a `pxweb_levels`
  object.

## Value

a `pxweb_levels` object
