# Build the PXWEB search URL

Build the PXWEB search URL

## Usage

``` r
pxweb_search_url(api_url, version)
```

## Arguments

- api_url:

  a PXWEB API root URL.

- version:

  PXWEB API version, `"v1"` or `"v2"`.
